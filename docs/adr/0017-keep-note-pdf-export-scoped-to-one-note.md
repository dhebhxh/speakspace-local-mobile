# Keep Note PDF export scoped to one Note

The complete iPhone PDF export includes full Ask AI messages only when the conversation's locked source set contains the exported Note alone. A conversation that also uses other Notes contributes its name, dates, and source count but not message bodies, because copying those messages into a single-Note archive could disclose content owned by the other sources; a future explicit conversation export may cover that broader scope without weakening the Note boundary.
